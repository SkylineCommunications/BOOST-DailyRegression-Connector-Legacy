name: Test PR Reusable Workflow - OIDC

on:
  push:
    tags:
      - "[0-9]+.[0-9]+.[0-9]+.[0-9]+"
      - "[0-9]+.[0-9]+.[0-9]+.[0-9]+-**"

  workflow_dispatch:

jobs:

  CI:
    uses: SkylineCommunications/_ReusableWorkflows/.github/workflows/Connector Master Workflow.yml@test-downstream
    with:
      sonarCloudProjectName: SkylineCommunications_BOOST-DailyRegression-Connector-Legacy